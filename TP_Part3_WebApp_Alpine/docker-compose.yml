version: "3"
services:
  javawebapp:
    image: timmesir/mditpdocker:lightweightimg
    deploy:
      replicas: 4
      restart_policy:
        condition: on-failure
    ports:
      - "8080:8080"
    networks:
      - webnet
networks:
  webnet: